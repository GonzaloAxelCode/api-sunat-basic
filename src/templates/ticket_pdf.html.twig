<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: Arial, sans-serif;
            font-size: 10px;
            color: #000;
            background: #fff;
            padding: 20px;
            line-height: 1.4;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: #fff;
            border: 2px solid #000;
            padding: 15px;
        }

        /* Header */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 15px;
            padding-bottom: 10px;
        }

        .company-section {
            flex: 1;
            padding-right: 20px;
        }

        .company-logo {
            margin-bottom: 8px;
        }

        .company-name {
            font-size: 16px;
            font-weight: bold;
            color: #000;
            margin-bottom: 5px;
        }

        .company-details {
            font-size: 9px;
            line-height: 1.5;
            color: #000;
        }

        /* Document Box */
        .document-box {
            border: 2px solid #000;
            padding: 12px 20px;
            text-align: center;
            min-width: 200px;
        }

        .ruc-text {
            font-size: 10px;
            font-weight: bold;
            margin-bottom: 3px;
        }

        .document-type {
            font-size: 13px;
            font-weight: bold;
            margin-bottom: 3px;
        }

        .document-number {
            font-size: 15px;
            font-weight: bold;
            letter-spacing: 0.5px;
        }

        /* Client Info Box */
        .client-box {
            border: 2px solid #000;
            padding: 10px;
            margin-bottom: 15px;
        }

        .client-title {
            font-size: 10px;
            font-weight: bold;
            margin-bottom: 8px;
            background: #f0f0f0;
            padding: 3px 5px;
        }

        .client-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 8px;
        }

        .client-row {
            display: flex;
            gap: 5px;
            font-size: 9px;
        }

        .client-row.full-width {
            grid-column: 1 / -1;
        }

        .client-label {
            font-weight: bold;
            min-width: 100px;
        }

        .client-value {
            flex: 1;
        }

        /* Items Table */
        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 15px;
            font-size: 9px;
        }

        .items-table thead {
            background: #f0f0f0;
        }

        .items-table th {
            padding: 6px 5px;
            text-align: center;
            font-weight: bold;
            border: 1px solid #000;
        }

        .items-table td {
            padding: 6px 5px;
            border: 1px solid #000;
            text-align: center;
        }

        .items-table td.text-left {
            text-align: left;
        }

        .items-table td.text-right {
            text-align: right;
        }

        /* Totals Section */
        .totals-section {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 15px;
        }

        .totals-table {
            border: 2px solid #000;
            font-size: 10px;
            min-width: 300px;
        }

        .totals-table td {
            padding: 5px 10px;
            border-bottom: 1px solid #000;
        }

        .totals-table tr:last-child td {
            border-bottom: none;
            font-weight: bold;
            font-size: 11px;
        }

        .totals-table td:first-child {
            text-align: left;
        }

        .totals-table td:nth-child(2) {
            text-align: center;
            width: 50px;
        }

        .totals-table td:last-child {
            text-align: right;
            width: 100px;
        }

        /* Amount in words */
        .amount-words {
            border: 1px solid #000;
            padding: 8px;
            margin-bottom: 15px;
            font-size: 9px;
        }

        .amount-words strong {
            font-weight: bold;
        }

        /* Transfer Guide */
        .transfer-section {
            border: 1px solid #000;
            padding: 8px;
            margin-bottom: 15px;
            font-size: 9px;
            text-align: center;
        }

        /* Footer */
        .footer {
            border-top: 2px solid #000;
            padding-top: 10px;
        }

        .footer-content {
            display: flex;
            gap: 15px;
            align-items: flex-start;
        }

        .footer-left {
            flex: 1;
            font-size: 8px;
            line-height: 1.5;
        }

        .footer-left strong {
            font-weight: bold;
        }

        .hash-text {
            word-break: break-all;
            font-family: 'Courier New', monospace;
            font-size: 7px;
        }

        .qr-section {
            text-align: center;
        }

        .qr-code {
            border: 1px solid #000;
            padding: 5px;
        }
    </style>
</head>
<body>
    {% set cp = doc.company %}
    {% set cl = doc.client %}
    {% set moneda = doc.tipoMoneda|catalog('02') %}
    {% set name = doc.tipoDoc|catalog('01') %}

    <div class="container">
        <!-- HEADER -->
        <div class="header">
            <div class="company-section">
                <div class="company-logo">
                    <img src="{{ params.system.logo|image_b64 }}" height="60">
                </div>
                <div class="company-name">{{ cp.razonSocial }}</div>
                <div class="company-details">
                    {{ cp.address.direccion }}<br>
                    {% if params.user.header is defined %}{{ params.user.header|raw }}{% endif %}
                </div>
            </div>
            <div class="document-box">
                <div class="ruc-text">RUC {{ cp.ruc }}</div>
                <div class="document-type">{{ name|upper }}</div>
                <div class="document-type">ELECTRÓNICA</div>
                <div class="document-number">{{ doc.serie }}-{{ doc.correlativo }}</div>
            </div>
        </div>

        <!-- CLIENT INFO -->
        <div class="client-box">
            <div class="client-title">DATOS DEL CLIENTE</div>
            <div class="client-grid">
                <div class="client-row">
                    <span class="client-label">RUC:</span>
                    <span class="client-value">{{ cl.numDoc }}</span>
                </div>
                <div class="client-row">
                    <span class="client-label">FECHA EMISIÓN:</span>
                    <span class="client-value">{{ doc.fechaEmision|date('d/m/Y') }}</span>
                </div>
                <div class="client-row">
                    <span class="client-label">DENOMINACIÓN:</span>
                    <span class="client-value">{{ cl.rznSocial }}</span>
                </div>
                <div class="client-row">
                    <span class="client-label">FECHA DE VENC.:</span>
                    <span class="client-value">{{ doc.fechaEmision|date('d/m/Y') }}</span>
                </div>
                <div class="client-row full-width">
                    <span class="client-label">DIRECCIÓN:</span>
                    <span class="client-value">{% if cl.address %}{{ cl.address.direccion }}{% else %}---{% endif %}</span>
                </div>
                <div class="client-row">
                    <span class="client-label">MONEDA:</span>
                    <span class="client-value">{{ moneda }}</span>
                </div>
            </div>
        </div>

        <!-- ITEMS TABLE -->
        <table class="items-table">
            <thead>
                <tr>
                    <th style="width: 50px;">CANT.</th>
                    <th style="width: 50px;">U/M</th>
                    <th style="width: 60px;">CÓD.</th>
                    <th>DESCRIPCIÓN</th>
                    <th style="width: 80px;">V/U</th>
                    <th style="width: 80px;">P/U</th>
                    <th style="width: 90px;">IMPORTE</th>
                </tr>
            </thead>
            <tbody>
                {% for det in doc.details %}
                <tr>
                    <td>{{ det.cantidad|n_format }}</td>
                    <td>{{ det.unidad|catalog('03') }}</td>
                    <td>{{ det.codProducto|default('001') }}</td>
                    <td class="text-left">{{ det.descripcion }}</td>
                    <td class="text-right">{{ det.mtoValorUnitario|n_format }}</td>
                    <td class="text-right">{{ det.mtoPrecioUnitario|n_format }}</td>
                    <td class="text-right">{{ det.mtoValorVenta|n_format }}</td>
                </tr>
                {% endfor %}
                {% for i in range(doc.details|length, 10) %}
                <tr>
                    <td>&nbsp;</td>
                    <td>&nbsp;</td>
                    <td>&nbsp;</td>
                    <td>&nbsp;</td>
                    <td>&nbsp;</td>
                    <td>&nbsp;</td>
                    <td>&nbsp;</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>

        <!-- TOTALS -->
        <div class="totals-section">
            <table class="totals-table">
                {% if doc.mtoOperGravadas %}
                <tr>
                    <td>GRAVADA</td>
                    <td>{{ moneda }}</td>
                    <td>{{ doc.mtoOperGravadas|n_format }}</td>
                </tr>
                {% endif %}
                {% if doc.mtoOperExoneradas %}
                <tr>
                    <td>EXONERADA</td>
                    <td>{{ moneda }}</td>
                    <td>{{ doc.mtoOperExoneradas|n_format }}</td>
                </tr>
                {% endif %}
                <tr>
                    <td>IGV 18.00 %</td>
                    <td>{{ moneda }}</td>
                    <td>{{ doc.mtoIGV|n_format }}</td>
                </tr>
                <tr>
                    <td>TOTAL</td>
                    <td>{{ moneda }}</td>
                    <td>{{ doc.mtoImpVenta|n_format }}</td>
                </tr>
            </table>
        </div>

        <!-- AMOUNT IN WORDS -->
        <div class="amount-words">
            <strong>IMPORTE EN LETRAS:</strong> {{ legend(doc.legends, '1000') }}
        </div>

        <!-- TRANSFER GUIDE -->
        <div class="transfer-section">
            <strong>GUÍA DE REMISIÓN REMITENTE:</strong> ( &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; )
        </div>

        <!-- FOOTER -->
        <div class="footer">
            <div class="footer-content">
                <div class="footer-left">
                    <strong>Representación impresa de la {{ name|upper }} ELECTRÓNICA, visita {{ params.user.website|default('www.sunat.gob.pe') }}</strong><br>
                    <strong>Autorizado mediante Resolución de Intendencia No 034-005-0005315</strong><br>
                    {% if params.system.hash is defined and params.system.hash %}
                    <strong>Resumen:</strong> <span class="hash-text">{{ params.system.hash }}</span>
                    {% endif %}
                </div>
                <div class="qr-section">
                    <img class="qr-code" src="{{ qrCode(doc)|image_b64('svg+xml') }}" width="120">
                </div>
            </div>
        </div>
    </div>
</body>
</html>