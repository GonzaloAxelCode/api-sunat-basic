<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            font-size: 11px;
            color: #1e293b;
            background: #ffffff;
            padding: 30px;
            line-height: 1.6;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: #ffffff;
        }

        /* Header moderno con gradiente sutil */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            padding-bottom: 25px;
            border-bottom: 3px solid #e2e8f0;
            margin-bottom: 30px;
        }

        .company-info {
            flex: 1;
        }

        .company-logo {
            margin-bottom: 12px;
        }

        .company-name {
            font-size: 18px;
            font-weight: 700;
            color: #0f172a;
            margin-bottom: 6px;
            letter-spacing: -0.02em;
        }

        .company-details {
            color: #64748b;
            font-size: 10px;
            line-height: 1.7;
        }

        .company-details strong {
            color: #475569;
            font-weight: 600;
        }

        /* Document box con diseño moderno */
        .document-box {
            background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
            color: white;
            padding: 20px;
            border-radius: 12px;
            text-align: center;
            min-width: 200px;
            box-shadow: 0 4px 6px -1px rgba(59, 130, 246, 0.2);
        }

        .document-type {
            font-size: 13px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 8px;
            opacity: 0.95;
        }

        .document-number {
            font-size: 20px;
            font-weight: 700;
            letter-spacing: 1px;
        }

        /* Secciones con diseño card */
        .section {
            margin-bottom: 25px;
        }

        .section-title {
            font-size: 12px;
            font-weight: 700;
            color: #0f172a;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 12px;
            padding-bottom: 6px;
            border-bottom: 2px solid #e2e8f0;
        }

        /* Info grid moderna */
        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
            background: #f8fafc;
            padding: 16px;
            border-radius: 8px;
            border: 1px solid #e2e8f0;
        }

        .info-item {
            display: flex;
            flex-direction: column;
        }

        .info-label {
            font-size: 9px;
            color: #64748b;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.3px;
            margin-bottom: 3px;
        }

        .info-value {
            font-size: 11px;
            color: #1e293b;
            font-weight: 500;
        }

        /* Tabla moderna */
        .items-table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
            margin-bottom: 20px;
            border-radius: 8px;
            overflow: hidden;
            border: 1px solid #e2e8f0;
        }

        .items-table thead {
            background: linear-gradient(180deg, #f1f5f9 0%, #e2e8f0 100%);
        }

        .items-table th {
            padding: 12px 10px;
            text-align: left;
            font-size: 10px;
            font-weight: 700;
            color: #475569;
            text-transform: uppercase;
            letter-spacing: 0.3px;
        }

        .items-table tbody tr {
            border-bottom: 1px solid #f1f5f9;
            transition: background-color 0.2s;
        }

        .items-table tbody tr:last-child {
            border-bottom: none;
        }

        .items-table tbody tr:hover {
            background-color: #f8fafc;
        }

        .items-table td {
            padding: 12px 10px;
            font-size: 10px;
            color: #334155;
        }

        .items-table .text-right {
            text-align: right;
            font-weight: 600;
            color: #1e293b;
        }

        /* Totales con diseño destacado */
        .totals-section {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 25px;
        }

        .totals-box {
            min-width: 320px;
            background: #f8fafc;
            border-radius: 8px;
            padding: 16px;
            border: 1px solid #e2e8f0;
        }

        .total-row {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            font-size: 11px;
        }

        .total-row.separator {
            border-top: 1px solid #e2e8f0;
            margin-top: 4px;
            padding-top: 12px;
        }

        .total-label {
            color: #64748b;
            font-weight: 600;
        }

        .total-value {
            color: #1e293b;
            font-weight: 600;
            text-align: right;
        }

        .total-row.final {
            background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
            color: white;
            margin: 8px -16px -16px -16px;
            padding: 14px 16px;
            border-radius: 0 0 8px 8px;
        }

        .total-row.final .total-label,
        .total-row.final .total-value {
            color: white;
            font-size: 13px;
            font-weight: 700;
        }

        /* Leyenda moderna */
        .legend-box {
            background: #fefce8;
            border-left: 4px solid #facc15;
            padding: 12px 16px;
            border-radius: 6px;
            margin-bottom: 25px;
        }

        .legend-box strong {
            color: #854d0e;
            font-size: 9px;
            text-transform: uppercase;
            letter-spacing: 0.3px;
            margin-right: 8px;
        }

        .legend-text {
            color: #713f12;
            font-size: 10px;
            font-weight: 500;
        }

        /* Footer moderno */
        .footer {
            margin-top: 35px;
            padding-top: 20px;
            border-top: 2px solid #e2e8f0;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: 1fr auto;
            gap: 20px;
            align-items: center;
        }

        .hash-section {
            background: #f8fafc;
            padding: 12px;
            border-radius: 6px;
            border: 1px solid #e2e8f0;
        }

        .hash-label {
            font-size: 9px;
            color: #64748b;
            font-weight: 600;
            text-transform: uppercase;
            margin-bottom: 4px;
        }

        .hash-value {
            font-family: 'Courier New', monospace;
            font-size: 8px;
            color: #475569;
            word-break: break-all;
        }

        .qr-section {
            text-align: center;
        }

        .qr-code {
            margin-bottom: 8px;
        }

        .footer-text {
            text-align: center;
            margin-top: 20px;
            padding-top: 15px;
            border-top: 1px solid #e2e8f0;
        }

        .footer-message {
            color: #3b82f6;
            font-weight: 600;
            font-size: 11px;
            margin-bottom: 4px;
        }

        .footer-legal {
            color: #94a3b8;
            font-size: 9px;
        }
    </style>
</head>
<body>
    {% set cp = doc.company %}
    {% set cl = doc.client %}
    {% set moneda = doc.tipoMoneda|catalog('02') %}
    {% set name = doc.tipoDoc|catalog('01') %}

    <div class="container">
        <!-- HEADER -->
        <div class="header">
            <div class="company-info">
                <div class="company-logo">
                    <img src="{{ params.system.logo|image_b64 }}" height="50">
                </div>
                <div class="company-name">{{ cp.razonSocial }}</div>
                <div class="company-details">
                    <strong>RUC:</strong> {{ cp.ruc }}<br>
                    <strong>Dirección:</strong> {{ cp.address.direccion }}<br>
                    {% if params.user.header is defined %}{{ params.user.header|raw }}{% endif %}
                </div>
            </div>
            <div class="document-box">
                <div class="document-type">{{ name }} Electrónica</div>
                <div class="document-number">{{ doc.serie }} - {{ doc.correlativo }}</div>
            </div>
        </div>

        <!-- INFORMACIÓN DEL CLIENTE -->
        <div class="section">
            <div class="section-title">Información del Cliente</div>
            <div class="info-grid">
                <div class="info-item">
                    <div class="info-label">Cliente</div>
                    <div class="info-value">{{ cl.rznSocial }}</div>
                </div>
                <div class="info-item">
                    <div class="info-label">{{ cl.tipoDoc|catalog('06') }}</div>
                    <div class="info-value">{{ cl.numDoc }}</div>
                </div>
                <div class="info-item">
                    <div class="info-label">Dirección</div>
                    <div class="info-value">{% if cl.address %}{{ cl.address.direccion }}{% else %}---{% endif %}</div>
                </div>
                <div class="info-item">
                    <div class="info-label">Fecha de Emisión</div>
                    <div class="info-value">{{ doc.fechaEmision|date('d/m/Y H:i') }}</div>
                </div>
            </div>
        </div>

        <!-- DETALLE DE PRODUCTOS -->
        <div class="section">
            <div class="section-title">Detalle de la Venta</div>
            <table class="items-table">
                <thead>
                    <tr>
                        <th style="width: 60px;">Cant.</th>
                        <th>Descripción</th>
                        <th style="width: 100px; text-align: right;">V. Unitario</th>
                        <th style="width: 100px; text-align: right;">Total</th>
                    </tr>
                </thead>
                <tbody>
                    {% for det in doc.details %}
                    <tr>
                        <td>{{ det.cantidad|n_format }}</td>
                        <td>{{ det.descripcion }}</td>
                        <td class="text-right">{{ moneda }} {{ det.mtoValorUnitario|n_format }}</td>
                        <td class="text-right">{{ moneda }} {{ det.mtoValorVenta|n_format }}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>

        <!-- TOTALES -->
        <div class="totals-section">
            <div class="totals-box">
                {% if doc.mtoOperGravadas %}
                <div class="total-row">
                    <div class="total-label">Op. Gravadas</div>
                    <div class="total-value">{{ moneda }} {{ doc.mtoOperGravadas|n_format }}</div>
                </div>
                {% endif %}
                {% if doc.mtoOperExoneradas %}
                <div class="total-row">
                    <div class="total-label">Op. Exoneradas</div>
                    <div class="total-value">{{ moneda }} {{ doc.mtoOperExoneradas|n_format }}</div>
                </div>
                {% endif %}
                <div class="total-row">
                    <div class="total-label">IGV (18%)</div>
                    <div class="total-value">{{ moneda }} {{ doc.mtoIGV|n_format }}</div>
                </div>
                <div class="total-row final">
                    <div class="total-label">IMPORTE TOTAL</div>
                    <div class="total-value">{{ moneda }} {{ doc.mtoImpVenta|n_format }}</div>
                </div>
            </div>
        </div>

        <!-- LEYENDA -->
        <div class="legend-box">
            <strong>Son:</strong>
            <span class="legend-text">{{ legend(doc.legends, '1000') }}</span>
        </div>

        <!-- FOOTER -->
        <div class="footer">
            <div class="footer-grid">
                {% if params.system.hash is defined and params.system.hash %}
                <div class="hash-section">
                    <div class="hash-label">Código Hash</div>
                    <div class="hash-value">{{ params.system.hash }}</div>
                </div>
                {% endif %}
                <div class="qr-section">
                    <img class="qr-code" src="{{ qrCode(doc)|image_b64('svg+xml') }}" width="100">
                </div>
            </div>
            
            <div class="footer-text">
                <div class="footer-message">¡Gracias por su compra!</div>
                <div class="footer-legal">Representación impresa de la {{ name }} ELECTRÓNICA</div>
            </div>
        </div>
    </div>
</body>
</html>