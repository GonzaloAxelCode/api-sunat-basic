<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Factura {{ invoice.serie }}-{{ invoice.correlativo }}</title>
    <style>
        /* General */
        body {
            font-family: "Helvetica", Arial, sans-serif;
            font-size: 12px;
            color: #333;
            margin: 0;
            padding: 0;
        }
        .container {
            padding: 20px;
            width: 100%;
        }

        /* Header */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            border-bottom: 2px solid #000;
            padding-bottom: 10px;
        }
        .company {
            font-weight: bold;
            font-size: 16px;
        }
        .company-info {
            font-size: 12px;
            color: #555;
        }
        .invoice-title {
            font-size: 24px;
            font-weight: bold;
            color: #2c3e50;
        }

        /* Cliente */
        .client-info {
            margin-bottom: 20px;
        }
        .client-info h4 {
            margin: 0;
            font-size: 14px;
        }
        .client-info p {
            margin: 2px 0;
            font-size: 12px;
        }

        /* Tabla de items */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        table th, table td {
            border: 1px solid #ccc;
            padding: 6px;
            text-align: left;
        }
        table th {
            background-color: #f5f5f5;
        }

        /* Totales */
        .totals {
            display: flex;
            justify-content: flex-end;
        }
        .totals table {
            width: 300px;
        }
        .totals table td {
            border: none;
            padding: 4px 6px;
        }
        .totals table tr.total td {
            font-weight: bold;
            border-top: 2px solid #000;
        }

        /* Footer */
        .footer {
            margin-top: 30px;
            font-size: 10px;
            text-align: center;
            color: #777;
        }
    </style>
</head>
<body>
<div class="container">
    <!-- Header -->
    <div class="header">
        <div class="company">
            {{ invoice.company.razonSocial }}
            <div class="company-info">
                RUC: {{ invoice.company.ruc }}<br>
                {{ invoice.company.address.direccion|default('') }}
            </div>
        </div>
        <div class="invoice-title">
            FACTURA<br>{{ invoice.serie }}-{{ invoice.correlativo }}
        </div>
    </div>

    <!-- Cliente -->
    <div class="client-info">
        <h4>Cliente:</h4>
        <p>{{ invoice.client.rznSocial }}</p>
        <p>{{ invoice.client.tipoDoc }}: {{ invoice.client.numDoc }}</p>
        <p>Dirección: {{ invoice.client.address.direccion|default('') }}</p>
        <p>Tel: {{ invoice.client.telephone|default('') }} | Email: {{ invoice.client.email|default('') }}</p>
    </div>

    <!-- Items -->
    <table>
        <thead>
            <tr>
                <th>Cod.</th>
                <th>Descripción</th>
                <th>Unidad</th>
                <th>Cant.</th>
                <th>Precio Unit.</th>
                <th>Valor Venta</th>
                <th>IGV</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
        {% for item in invoice.details %}
            <tr>
                <td>{{ item.codProducto }}</td>
                <td>{{ item.descripcion }}</td>
                <td>{{ item.unidad }}</td>
                <td>{{ item.cantidad }}</td>
                <td>{{ item.mtoPrecioUnitario|number_format(2, '.', ',') }}</td>
                <td>{{ item.mtoValorVenta|number_format(2, '.', ',') }}</td>
                <td>{{ item.igv|number_format(2, '.', ',') }}</td>
                <td>{{ item.mtoValorVenta + item.igv|number_format(2, '.', ',') }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <!-- Totales -->
    <div class="totals">
        <table>
            <tr>
                <td>Subtotal:</td>
                <td>{{ invoice.subTotal|default(0)|number_format(2, '.', ',') }}</td>
            </tr>
            <tr>
                <td>IGV ({{ invoice.tipoMoneda }}):</td>
                <td>{{ invoice.mtoIGV|default(0)|number_format(2, '.', ',') }}</td>
            </tr>
            <tr class="total">
                <td>Total:</td>
                <td>{{ invoice.mtoImpVenta|default(0)|number_format(2, '.', ',') }}</td>
            </tr>
        </table>
    </div>

    <!-- Leyenda -->
    <p><strong>Leyenda:</strong> {{ invoice.legends[0].value|default('') }}</p>

    <!-- Footer -->
    <div class="footer">
        Gracias por su preferencia. Este documento es una representación impresa de la factura electrónica.
    </div>
</div>
</body>
</html>
