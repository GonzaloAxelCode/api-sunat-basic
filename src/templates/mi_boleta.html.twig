<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: "DejaVu Sans", sans-serif;
            font-size: 11px;
            margin: 0;
            padding: 0;
            background: #111;
            color: #fff;
            width: 80mm;
        }
        .header {
            text-align: center;
            padding: 5px 0;
        }
        .header img {
            max-width: 60px;
            margin-bottom: 5px;
        }
        .company-name {
            font-size: 14px;
            font-weight: bold;
        }
        .ruc {
            font-size: 12px;
        }
        .divider {
            border-top: 1px dashed #555;
            margin: 5px 0;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th {
            text-align: left;
            font-size: 11px;
            border-bottom: 1px dashed #555;
            padding-bottom: 2px;
        }
        td {
            padding: 2px 0;
            vertical-align: top;
        }
        .total {
            font-weight: bold;
            font-size: 12px;
        }
        .footer {
            text-align: center;
            margin-top: 5px;
            font-size: 10px;
        }
    </style>
</head>
<body>

<div class="header">
    {% if company.logo %}
        <img src="{{ company.logo }}" alt="Logo">
    {% endif %}
    <div class="company-name">{{ company.name }}</div>
    <div class="ruc">RUC: {{ company.ruc }}</div>
    <div>{{ company.address }}</div>
    <div>{{ company.department }} - {{ company.province }}</div>
</div>

<div class="divider"></div>

<div style="text-align: center;">
    <strong>{{ document.tipoDocDesc }}</strong><br>
    {{ document.serie }} - {{ document.correlativo }}
</div>

<div class="divider"></div>

<p>Fecha: {{ document.fechaEmision|date("d/m/Y H:i") }}</p>
<p>Cliente: {{ client.rznSocial }}</p>
<p>Documento: {{ client.tipoDoc }} {{ client.numDoc }}</p>

<div class="divider"></div>

<table>
    <thead>
        <tr>
            <th>Cant</th>
            <th>Descripción</th>
            <th style="text-align:right;">P. Unit</th>
            <th style="text-align:right;">Total</th>
        </tr>
    </thead>
    <tbody>
        {% for item in details %}
        <tr>
            <td>{{ item.quantity }}</td>
            <td>{{ item.description }}</td>
            <td style="text-align:right;">{{ item.unitPrice|number_format(2) }}</td>
            <td style="text-align:right;">{{ item.total|number_format(2) }}</td>
        </tr>
        {% endfor %}
    </tbody>
</table>

<div class="divider"></div>

<table>
    <tr>
        <td>Op. Gravada:</td>
        <td style="text-align:right;">{{ totals.opGravadas|number_format(2) }}</td>
    </tr>
    <tr>
        <td>IGV (18%):</td>
        <td style="text-align:right;">{{ totals.igv|number_format(2) }}</td>
    </tr>
    <tr class="total">
        <td>Total:</td>
        <td style="text-align:right;">{{ totals.total|number_format(2) }}</td>
    </tr>
</table>

<div class="divider"></div>

<p><strong>Son: {{ totals.leyenda }}</strong></p>

{% if document.hash %}
    <p>Hash: {{ document.hash }}</p>
{% endif %}

<div class="footer">
    ¡Gracias por su compra!<br>
    Representación impresa de la {{ document.tipoDocDesc }}
</div>

</body>
</html>
